# Docker-specific environment configuration
# Used when building and running the Docker container

# ==========================================
# API Configuration
# ==========================================
API_HOST=0.0.0.0
API_PORT=8000

# ==========================================
# Paths (Docker internal paths)
# ==========================================
VECTORSTORE_DIR=/app/vectorstore
DATA_DIR=/app/data
CHROMA_COLLECTION_NAME=ng12_guidelines

# ==========================================
# Google Cloud / Vertex AI
# ==========================================
# Set these in docker-compose.yml or via 'docker run -e' flag
# GCP_PROJECT_ID=your_project_id
# GCP_LOCATION=location

# ==========================================
# Model Configuration
# ==========================================
EMBEDDING_MODEL=text-embedding-004
LLM_MODEL=gemini-2.5-pro
EMBEDDING_DIMENSION=768

# ==========================================
# Chunking Parameters
# ==========================================
CHUNK_SIZE_TOKENS=512
CHUNK_OVERLAP_TOKENS=100
MIN_CHUNK_SIZE_TOKENS=50

# ==========================================
# Retrieval Settings
# ==========================================
DEFAULT_TOP_K=5
SIMILARITY_THRESHOLD=0.7

# ==========================================
# Session Settings
# ==========================================
MAX_SESSION_HISTORY=10
SESSION_TIMEOUT_MINUTES=60

# ==========================================
# Mode (see README for details)
# ==========================================
# Set to 'true' to use mock services (no GCP credentials needed)
# Set to 'false' for full services with Vertex AI
USE_MOCK=true

# ==========================================
# Logging
# ==========================================
LOG_LEVEL=INFO

# ==========================================
# Performance
# ==========================================
WORKERS=4
